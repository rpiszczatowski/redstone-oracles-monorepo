{
  "name": "redstone-streamr-proxy",
  "prettier": "redstone-prettier-config",
  "version": "1.0.5",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "license": "MIT",
  "scripts": {
    "test": "NODE_ENV=test jest",
    "build": "rm -rf dist && tsc --build"
  },
  "dependencies": {
    "pako": "^2.1.0",
    "streamr-client": "^7.1.0"
  },
  "devDependencies": {
    "@types/jest": "^27.5.2",
    "@types/pako": "^2.0.0",
    "jest": "^28.1.0",
    "ts-jest": "^28.0.3"
  }
}
